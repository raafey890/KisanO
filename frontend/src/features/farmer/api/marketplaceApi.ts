import { FARMER_ENDPOINTS } from './farmerEndpoints';

// TODO: Backend integration pending for Marketplace Module
// Implement full API methods here once backend is ready
export const marketplaceApi = {
  getProducts: async () => {
    // return await api.get(FARMER_ENDPOINTS.MARKETPLACE);
    return [];
  },
};
