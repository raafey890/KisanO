import { useQuery } from '@tanstack/react-query';

// TODO: Backend integration pending for useAiDoctor Module
// Implement React Query hooks here once backend is ready
export const useAiDoctor = () => {
  // return useQuery(['aidoctor'], ...);
};

